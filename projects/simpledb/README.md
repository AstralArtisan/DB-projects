# SimpleDB 实验专题

`proj2/` 是 SimpleDB 查询执行与优化实验，和 `projects/ecommerce/` 中的 Olist 电商数据库专题相互独立。

当前目录保留 Project2 的代码、数据、结果文件和运行说明。后续修改 SimpleDB 实验时，优先在 `proj2/` 内维护，不要引入对 `projects/ecommerce/shared/` 的依赖。
